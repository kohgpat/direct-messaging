import React from "react";
import * as s from "./styles";

const DialoguesHeader = () => (
  <s.DialoguesHeader>
    <s.SearchInputBar>
      <s.SearchInputIcon />
      <s.SearchInput />
    </s.SearchInputBar>
  </s.DialoguesHeader>
);

export default DialoguesHeader;
